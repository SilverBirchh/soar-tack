{{result.playerOne.name}} {{result.playerOne.score}} vs {{result.playerTwo.score}} {{result.playerTwo.name}}

<button {{ action "delete" result.id }}type="button" class="btn btn-danger btn-xs pull-right" aria-label="Left Align">
  <span class="glyphicon glyphicon-minus" aria-hidden="true"></span>
</button>
